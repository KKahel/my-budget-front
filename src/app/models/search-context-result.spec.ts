import { SearchContextResult } from './search-context-result';

describe('SearchContextResult', () => {
  it('should create an instance', () => {
    expect(new SearchContextResult()).toBeTruthy();
  });
});
